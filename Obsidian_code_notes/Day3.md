
# 写在前面

承接昨天没看完的第二章

# 2.2变量

**定义 vs 声明**：
- **定义** = 类型 + 由变量名组成的列表（变量名用逗号分隔），以分号结束
- **声明** = 告诉编译器"这名字在别处定义了"，用 `extern`

```cpp
extern int i;       // 声明
int j;              // 定义
extern int k = 1;   // 定义（有初始化就抵消了 extern）
```

## 2.2.1 初始化的常见形式
```cpp
int a = 0;   // 拷贝初始化
int b(0);     // 直接初始化
int c{0};     // 列表初始化（C++11）—— 丢数据就报错
int d = {0};  // 同上
```

> **列表初始化的好处**：`int x{3.14}` 会报错（窄化转换），`int x(3.14)` 静默截断成 3。

## 2.2.2 默认初始化
- 函数外定义的变量 → 0
- 函数内定义的内置类型 → **未定义**（什么垃圾值都有）
- 类对象 → 由构造函数决定

> 函数内定义的 `int sum = 0;` 不要偷懒写成 `int sum;`。

## 2.2.3 何为对象

（不是谈恋爱的对象www）

对象，object，是指一块能存储数据并具有某种类型的内存空间

简单来说，对象就是具有某种数据类型的内存空间

> 注意：初始化不是赋值，其真正含义是创建时赋予变量一个初始值
> 而赋值的含义是把对象当前值擦除，用一个新值替代。

## 2.2.4 静态类型语言

>书中提到了C++的语言类型，详见[[编程语言类型]]

## 2.2.5 标识符

identifier 
由字母，数字，下划线组成

- 必须以字母或下划线开头
- 标识符不能连续出现两个下划线
- 不能以下划线紧接大写字母开头
- 定义在函数体外的标识符也不能以下划线开头

书中提到了C++关键字，有不少我还不认识，(让ds v4老师)单独列出来写进[[C++关键字表]]

## 2.2.6 名字的作用域

作用域(scope)

C++中大多数作用域以花括号分隔

### **全局作用域(global scope)**
整个程序范围内有用

### **块作用域(block scope)**
只在所在块内起作用

### **嵌套作用域**





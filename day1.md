# Day1
## 1.iostream
```cpp
#include <iostream>
/* iostream 是 iostream 和 ostream
 分别表示输入流和输出流
 一个流是一个字符序列，stream想要表达的是：
 随着时间的推移，字符是顺序生成或消耗的
 除了 cin 和 cout 还有 cerr 和 clog
*/
int main(){
    std::cout << "Hello,world! " << std::endl;
    return 0;
}
```
## 2.cout cin << >>
```cpp
int main(){
    int v1 = 0,v2 = 0;
    std::cin >> v1 >> v2;  
    std::cout << v1 << v2;
    return 0;
}
/**
 * 这里的>>是输入运算符，接受一个istream作为左侧运算对象
 * >> 从istream读入数据，并存入后面的给定对象中，所以，数据的传递方向是向右
 * >> 的方向也是向右，由此理解
 * 需要注意的是 : >> 返回左侧运算对象作为计算结果，因此返回 istream
 * 这也是为什么了可以连续用两个 >>
 * 等价于 (std::cin >> v1 ) >> v2 ;
 * 
 * cout 和 << 同理，只是ostream而已，不多赘述
 * 
 * 
/
```

## 3.控制流

### 3.1 while语句
反复执行一段代码，知道给定条件为假时为止

### 3.2 for语句
在循环条件中检测变量，在循环体中定义、改变变量
注意循环头的末尾，变量改变是在循环体结束之后改变

### 3.3 for 和 while 对比分析

> **选择原则：知道循环次数用 `for`，只知道退出条件用 `while`。**

| 维度 | for | while |
|------|------|-------|
| 结构 | 初始化、条件、更新集中在循环头 | 只有条件在循环头，初始化和更新分散 |
| 循环变量 | 循环体内有效，结束即销毁 | 循环体外仍可访问 |
| 遗漏更新风险 | 低（循环头强制写更新） | 高（需手动在循环体内更新） |
| 适合场景 | 计数遍历、固定范围 | 未知次数、等待条件、流读取 |
| 灵活性 | 低 | 高 |

**示例：**
```cpp
// for：知道循环范围
for(int i = 1; i <= 100; i++) { sum += i; }

// while：知道退出条件，但不知道跑几轮
while(std::cin >> word) { ... }           // 读到文件末尾
while(n1 == n2) { cin >> n1 >> n2; }      // 等到用户输入合法值
```

### 3.4 循环的应用：读取数量不定的输入数据

```cpp
//实现对输入数字求和
int val = 0,sum = 0;
while(std::cin >> val){
    sum += val;
}
```

### 3.5 if  语句

if 除了常见的条件判断，其对 形如

```cpp
if(std::cin >> val){
    ...
}
```

也可以作用

对括号内进行“求值”，如果`std::cin`读取成功（即有输入），那么条件为真，继续执行后面的语句块

## 4 报错类型分类

从代码到可执行程序，错误按阶段分为五类：

### 4.1 语法错误 (Syntax Error)
代码违反 C++ 语法规则，编译器无法解析。**不会生成 .exe。**
- 漏分号、漏括号、关键字拼错、引号未闭合等
- 编译器会报行号，直接定位修改

### 4.2 语义错误 (Semantic Error)
语法正确，但"说不通"——类型不匹配、使用未声明变量、函数参数不对等。**编译期发现，不会生成 .exe。**

### 4.3 链接错误 (Linker Error)
编译通过（.o 已生成），但链接阶段找不到函数定义。**不会生成 .exe。**
- `undefined reference`：只声明了函数没写实现
- `multiple definition`：同一个函数定义了多次

### 4.4 运行时错误 (Runtime Error)
编译链接全部通过，**运行中崩溃**。
- 除零、空指针、数组越界、栈溢出、递归过深等
- 程序直接终止，不输出结果

### 4.5 逻辑错误 (Logic Error)
不报错、不崩溃，但**结果不对**。最难排查。
- `=` 写成 `==`、循环边界差一、浮点比较等

## 5.Sales_item类

---

## 6. Day1 学习总结

### 今日知识点

1. **输入输出流** — `iostream` 包含 `istream`（cin）和 `ostream`（cout、cerr、clog），流是随时间顺序生成/消耗的字符序列。
2. **`>>` 和 `<<` 运算符** — 返回左侧运算对象（`istream` / `ostream`），所以可以连续使用。`>>` 读取前会**跳过所有空白符**（空格、换行、制表符）。
3. **while 和 for** — 知道循环次数用 `for`，只知道退出条件用 `while`。for 结构紧凑、变量不泄漏；while 灵活、适合等待条件。
4. **不定数量输入** — `while(std::cin >> val)` 靠 EOF（Windows：`Ctrl+Z`）终止输入，因为 `>>` 会把换行当空白跳过，按 Enter 不会退出循环。
5. **if 对表达式求值** — `if(std::cin >> val)` 会执行读取操作，成功返回真。
6. **单引号 vs 双引号** — 单引号包**一个字符**（`'a'`），双引号包**字符串**（`"abc"`）。用单引号包多个字符会产生 multi-character literal 警告。
7. **报错五类型** — 语法错误 / 语义错误 / 链接错误 / 运行时错误 / 逻辑错误。

### 今日练习

| 测试 | 内容 | 踩坑 |
|------|------|------|
| test1 | 注释与字符串的边界辨析 | — |
| test2 | while → for 改写（求和 50~100） | — |
| test3 | while → for 改写（倒序 10~0） | — |
| test4 | 输入两数，打印中间值，处理非法输入 | 递归后没用 `else`，导致旧值再跑一遍 |
| test5 | 不定数量输入求和 | 按 Enter 不会结束，需要 `Ctrl+Z` |
| test6 | 统计连续相同值的出现次数 | 单引号误包多字符 |

### 今日啃书进度

C++ Primer 第一章基本结束，该注意的地方也注意了，还是学到了不少之前没学到的东西的

不得不说，之前学的C++都太肤浅，太粗略了。。。

python的书今天还没看。。。（在心里给自己两个耳光）

### 写在最后

今天的学习总体不错，就是效率有点低了

github的初步使用我已经了解，把学习进展放进github真是个明智的选择

（就是编写学习笔记有点麻烦。。。哎，我还是有点懒）

今天只学了c++，还没学python，明天得合理安排一下



今天的自己比昨天要努力，比昨天进步了，向自己致敬

respect



就到这里www


# Day2

## 写在前面

今天开始进入C++ Primer 的Chapter2；然后Python也要开始看了，哪里薄弱看哪里吧

2026/8/6

## *C++*

### Chapter 2 变量和基本类型

#### 2.1 算术类型

分为两类：**整型**（包括字符和布尔型）和**浮点型**。

| 类型 | 含义 | 最小尺寸 |
|------|------|----------|
| `bool` | 布尔 | — |
| `char` | 字符 | 8位 |
| `short` | 短整型 | 16位 |
| `int` | 整型 | 16位（通常32位） |
| `long` | 长整型 | 32位 |
| `long long` | 长长整型 | 64位 |
| `float` | 单精度浮点 | 6位有效数字 |
| `double` | 双精度浮点 | 10位有效数字 |
| `long double` | 扩展精度浮点 | 10位有效数字 |

> 实际大小因编译器和平台而异。**C++ 只保证 `short ≤ int ≤ long ≤ long long`。**

**带符号和无符号**：
- 默认带符号（`signed`），如 `int` = `signed int`
- 无符号加 `unsigned`：`unsigned int`，范围 0 ~ 2^n-1
- 混用有/无符号会有意料之外的类型转换，**建议统一用有符号**（除了位运算场景）

**类型选择经验**：
- 整数就 `int`，不够大换 `long long`
- 浮点就 `double`，精度和性能比较均衡
- 别用 `float` 除非内存特别紧
- 字符就 `char`，别用 `bool` 存字符

**Unicode 字符**：

`char` 只有 1 字节，最多表示 256 种字符，对于英文够用，但碰上中文、日文、emoji 就完全不够了。Unicode 就是用来解决"全世界文字统一编码"的。

**两个概念辨析**：

- **Unicode** — 字符集，给每个字符分配一个唯一编号（"码点"），如 `U+4E2D` = "中"
- **UTF-8 / UTF-16 / UTF-32** — 编码方式，决定"码点怎么存成字节"

C++ 为 Unicode 提供了几种扩展字符类型：

| 类型 | 大小 | 用途 |
|------|------|------|
| `wchar_t` | 2 或 4 字节（平台相关） | 宽字符，老方案 |
| `char16_t` | 2 字节 | UTF-16 |
| `char32_t` | 4 字节 | UTF-32，直接存码点 |

```cpp
char16_t c = u'中';       // u'' 前缀 = UTF-16 字面量
char32_t c2 = U'中';      // U'' 前缀 = UTF-32 字面量
```

> **实际开发中**：UTF-8 最通用（英文字符 1 字节，中文 3 字节），在 C++ 里直接存在 `std::string`（本质是 `char` 序列）里就行，日常用不到 `char16_t` 除非做跨平台文本处理。

#### 2.2 字面值常量

```cpp
20          // 十进制 int
024         // 八进制 → 20
0x14        // 十六进制 → 20
3.14        // double
'a'         // char
"hello"     // 字符串（末尾自动加 '\0'）
true        // bool
```

**转义序列**：`\n`（换行）`\t`（制表）`\\`（反斜杠）`\"`（双引号）`\'`（单引号）

#### 2.3 变量

**定义 vs 声明**：
- **定义** = 类型 + 名字 + 分配内存（可选初始化）
- **声明** = 告诉编译器"这名字在别处定义了"，用 `extern`

```cpp
extern int i;       // 声明
int j;              // 定义
extern int k = 1;   // 定义（有初始化就抵消了 extern）
```

**初始化 ≠ 赋值**：
```cpp
int a = 0;   // 拷贝初始化
int b(0);     // 直接初始化
int c{0};     // 列表初始化（C++11）—— 丢数据就报错
int d = {0};  // 同上
```

> **列表初始化的好处**：`int x{3.14}` 会报错（窄化转换），`int x(3.14)` 静默截断成 3。

**默认初始化**：
- 函数外定义的变量 → 0
- 函数内定义的内置类型 → **未定义**（什么垃圾值都有）
- 类对象 → 由构造函数决定

> 函数内定义的 `int sum = 0;` 不要偷懒写成 `int sum;`。

#### 2.4 引用（`&`）

**引用就是别名**，绑定后不能换绑到别的变量。

```cpp
int i = 0;
int &r = i;    // r 是 i 的别名
r = 10;        // i 也变成 10
```

- 引用必须初始化，不能空着
- 引用和原变量**类型必须一致**（`double` 不能绑到 `int&` 上）
- 引用不是对象，没有自己的地址

#### 2.5 指针（`*`）

和引用的区别：指针是**独立的对象**，存的是地址。

```cpp
int i = 42;
int *p = &i;    // p 存 i 的地址
*p = 10;        // 解引用，i 变成 10
```

| | 引用 | 指针 |
|------|------|------|
| 本质 | 别名 | 独立对象（存地址） |
| 必须初始化 | 是 | 否（但别留空） |
| 能否换绑 | 否，终身绑定 | 是，可指别处 |
| 是否占内存 | 否 | 是 |

**空指针**（C++11）：
```cpp
int *p = nullptr;   // 推荐，专用于指针
int *q = NULL;      // 老写法，本质是 0
int *r = 0;         // 也行但不推荐
```

**`void*` 指针**：可以存任意类型的地址，但不能直接解引用——不知道指向对象的类型。

#### 2.6 const 限定符

值不能修改：
```cpp
const int i = 42;
i = 10;            // ❌ 编译错误
```

**const 和引用**：`const int &` 可以绑到字面量、表达式、不同类型：
```cpp
const int &r = 42;               // OK
const double &d = 3.14;          // OK
```

**const 和指针**：分两种——
```cpp
const int *p = &i;    // 指向常量的指针：不能通过 p 改 i
int *const p = &i;     // 常量指针：p 不能指别的东西
```

> 记忆口诀：`*` 前面有 const → 指向的不能改；`*` 后面有 const → 指针本身不能改。

#### 2.7 auto 和 decltype

**`auto`**：让编译器推导类型，`=` 右边的类型是什么就是什么。
```cpp
auto i = 42;         // int
auto d = 3.14;       // double
auto s = "hello";    // const char*
```

> 注意 `auto` 默认**忽略顶层 const 和引用**：
> ```cpp
> const int ci = 0;
> auto x = ci;        // int，const 丢了
> auto &y = ci;       // const int&，保留引用需要显式写 &
> ```

**`decltype`**：不计算表达式，只取它的类型。
```cpp
int i = 0;
decltype(i) j = 10;  // j 是 int
decltype((i)) k = i; // 注意：(i) 是表达式，结果是 int&
```

> `decltype` vs `auto`：auto 会算值然后推类型；decltype 只看类型不算值。



